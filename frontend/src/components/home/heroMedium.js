
export const HeroMedium = (props) => {
    return(
        <section className="hero is-medium">
            <div className="hero-body">
                {props.clidren}
            </div>
        </section>

    );
}