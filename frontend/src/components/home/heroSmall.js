
export const HeroSmall = (props) => {
    return(
        <section className="hero is-small">
            <div className="hero-body">
                {props.clidren}
            </div>
        </section>

    );
}